{
  "name": "tranchnode",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit && npm run room:check && node --test --import tsx test/**/*.test.ts",
    "test": "node --test --import tsx test/**/*.test.ts",
    "intent-stroke:stdio": "node --import tsx scripts/intent-stroke-stdio.ts",
    "room:check": "node --import tsx scripts/room-check.ts",
    "room:render": "node --import tsx scripts/room-render.ts"
  },
  "dependencies": {
    "canonicalize": "^2.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
